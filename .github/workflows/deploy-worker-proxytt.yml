name: Deploy Worker (proxytt)

on:
  push:
    branches: [main]
    paths:
      - "workers/**"
      - "wrangler.toml"
      - ".github/workflows/deploy-worker-proxytt.yml"
  workflow_dispatch:

concurrency:
  group: deploy-worker-proxytt
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Deploy
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: 84a8fceb1aac6b1b1a4a141d7c791f78
          command: deploy --keep-vars
